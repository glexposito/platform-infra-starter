name: Deploy
run-name: Deploy (${{ inputs.environment }} / ${{ inputs.command }})

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Environment to deploy"
        required: true
        type: choice
        options:
          - dev
          - prod
      command:
        description: "Terraform command"
        required: true
        type: choice
        options:
          - plan
          - apply

permissions:
  contents: read
  id-token: write

env:
  TERRAFORM_VERSION: "1.14.7"

jobs:
  prepare_deployment:
    name: Prepare Deployment
    runs-on: ubuntu-latest
    outputs:
      environment_name: ${{ steps.resolve.outputs.environment_name }}
      tfvars_file: ${{ steps.resolve.outputs.tfvars_file }}
      backend_config: ${{ steps.resolve.outputs.backend_config }}
    steps:
      - name: Resolve deployment config
        id: resolve
        env:
          INPUT_ENVIRONMENT: ${{ inputs.environment }}
        run: |
          python3 <<'PY' >> "$GITHUB_OUTPUT"
          import os
          import sys

          environment = os.environ["INPUT_ENVIRONMENT"]

          env_mapping = {
              "dev": {
                  "environment_name": "dev",
                  "tfvars_file": "env/dev/deploy.tfvars",
                  "backend_config": "backend/dev.tfbackend",
              },
              "prod": {
                  "environment_name": "prod",
                  "tfvars_file": "env/prod/deploy.tfvars",
                  "backend_config": "backend/prod.tfbackend",
              },
          }

          if environment not in env_mapping:
              print(f"Unsupported environment: {environment}", file=sys.stderr)
              sys.exit(1)

          for key, value in env_mapping[environment].items():
              print(f"{key}={value}")
          PY

  deploy:
    needs: prepare_deployment
    runs-on: ubuntu-latest
    name: Deploy (${{ needs.prepare_deployment.outputs.environment_name }})
    environment: ${{ needs.prepare_deployment.outputs.environment_name }}
    concurrency:
      group: deploy-${{ needs.prepare_deployment.outputs.environment_name }}
      cancel-in-progress: false
    env:
      ARM_USE_OIDC: true
      ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
      ARM_TENANT_ID: ${{ vars.AZURE_TENANT_ID }}
      ARM_SUBSCRIPTION_ID: ${{ vars.AZURE_SUBSCRIPTION_ID }}
      TF_VAR_secure_environment_variables: '{"AZURE_CLIENT_ID":"${{ secrets.AZURE_CLIENT_ID }}"}'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Azure login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TERRAFORM_VERSION }}

      - name: Terraform init
        run: terraform init -backend-config="${{ needs.prepare_deployment.outputs.backend_config }}"

      - name: Terraform plan
        run: terraform plan -var-file="${{ needs.prepare_deployment.outputs.tfvars_file }}"

      - name: Terraform apply
        if: inputs.command == 'apply'
        run: terraform apply -auto-approve -var-file="${{ needs.prepare_deployment.outputs.tfvars_file }}"
